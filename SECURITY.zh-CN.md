# 安全政策

[English](./SECURITY.md) | [简体中文](./SECURITY.zh-CN.md)

Protofire 是一个研究级的裸机内核原型。安全性受到认真对待：内核实现了
PAN/SMAP/SUM、Biba 完整性模型、MAC 类型强制、seccomp 过滤、每线程栈金丝雀、
审计子系统以及静态加密（AES-256-XTS + LUKS2）。但请注意，这是一个研究项目——
**请不要在生产环境中运行不可信的工作负载**。

## 受支持的版本

| 版本 | 支持 |
|------|------|
| `main`（开发版） | 持续修复 |
| `1.0.x`（最新发布版） | 安全修复与回移 |
| 更早的版本 | 不支持 |

## 报告漏洞

**请不要为安全漏洞提交公开 Issue。**

如需私下报告漏洞：

1. **首选：** 使用 GitHub 的**私有漏洞报告**功能（仓库 **Security** 标签页 →
   **Report a vulnerability**）。只有维护者能看到该报告。
2. **备选：** 提交一个标题以 `SECURITY:` 开头的普通 Issue，**不要**包含任何利用
   细节，我们会直接联系你。

报告中请包含：

- 受影响架构（`x86_64` / `aarch64` / `riscv64`），以及相关的特性标志（如适用）。
- 运行环境（QEMU 型号与版本，或真实硬件）。
- 最小复现：启动命令、内核构建参数、操作步骤。
- 预期与实际行为的差异，以及任何 panic/日志输出。
- 如果你有判断，附上严重性/影响评估。

你将在 **5 个工作日内** 收到确认，并在问题被分诊和修复的过程中收到进展更新。

## 披露政策

- 我们遵循 **90 天协同披露**窗口：在修复可用之前，请报告者不要公开细节。
- 修复先落在 `main`，然后回移到最新受支持的版本（`1.0.x`）。
- 除非报告者要求匿名，否则我们会在公告中致谢。

## 范围

**范围内：** `src/` 下的内核源码、构建系统（`Makefile`、`build.rs`、链接脚本），
以及 `src/user/shared/` 下的用户 ABI 实现。

**范围外：** 宿主机工具链、QEMU 本身、宿主操作系统，以及任何未随本仓库 vendored
的第三方 crate。

## 漏洞赏金

本项目目前不提供漏洞赏金。我们感谢研究人员本着打造更好内核的初衷进行负责任的
披露。
